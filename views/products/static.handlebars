<ul class="productList">
    {{#each this}} {{#if product_name}}
    <li>
        <div class="productContainer">
            <h2>{{product_name}}</h2>
            <img src="{{product_picture}}" />
            <p>UPC: {{_id}}</p>
        </div>
    </li>
    {{/if}} {{/each}}
</ul>